'use client';
import { CircularProgress } from '@mui/material';

export interface LsdProps {}

export const Lsd: FCC<LsdProps> = ({}) => {
  return (
    <>
      <CircularProgress />
    </>
  );
};
